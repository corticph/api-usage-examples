{
  "name": "corti-ambientscribe-demo",
  "version": "1.0.0",
  "description": "Live audio transcription and clinical document generation demo using Corti SDK",
  "main": "server.ts",
  "type": "module",
  "scripts": {
    "build:client": "esbuild client.ts --bundle --outfile=dist/client.js --format=iife --global-name=AmbientScribe",
    "dev": "npm run build:client && ts-node --esm server.ts",
    "start": "node dist/server.js",
    "build": "npm run build:client && tsc",
    "clean": "rm -rf dist"
  },
  "keywords": ["corti", "audio", "transcription", "clinical-documentation"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@corti/sdk": "^0.10.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.6",
    "esbuild": "^0.24.0",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2"
  }
}
